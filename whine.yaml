rulePackages:
  - prelude
  - whine-core:
      local: ./
      module: Whine.Core.WhineRules

include:
  - src/**/*.purs
  - bootstrap/**/*.purs
  - vscode-extension/**/*.purs
  - test/**/*.purs

rules:
  UndesirableFunctions:
    functions:
      unsafeCoerce: Do not use `unsafeCoerce`
